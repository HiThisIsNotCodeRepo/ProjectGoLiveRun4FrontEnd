import { Route } from '@angular/router';
import { AuthSignOutComponent } from 'app/paotui/component/sign-out/sign-out.component';

export const authSignOutRoutes: Route[] = [
    {
        path     : '',
        component: AuthSignOutComponent
    }
];
